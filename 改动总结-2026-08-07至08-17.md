# 改动总结：2026-08-07 至 2026-08-17

范围：商店通知优化（8-7）→ 队友系统（8-13/14）→ 售出系统（8-17）→ 商店稀有度类目改造（8-17）→ 售出绿色配对约束（8-17）。
提交共 37 个（均已合并本地 main）；代码统计：约 56 个文件，+3514 / -347 行（不含文档与未提交产物）。

## 总览

| 模块 | 日期 | 提交 | 规模 | 状态 |
|---|---|---|---|---|
| 商店通知优化 | 08-07 | 无独立提交 | 小 | 已并入后续代码 |
| 队友系统 | 08-13 | 12 | 17 文件 +1232/-23 | 已合并 main |
| 修复与杂项 | 08-14 | 5 | 9 文件 +100/-61 | 已合并 main |
| 售出系统 | 08-17 | 8（含文档） | 18 文件 +1170/-157 | 已合并 main |
| 商店稀有度改造 | 08-17 | 6（含文档） | 11 文件 +693/-32 | 已合并 main |
| 售出绿色配对 | 08-17 | 6（含文档） | 7 文件 +319/-74 | 已合并 main |

---

## 一、商店通知优化（8-7）

- 去掉商店库存变化时对全体玩家的弹窗提示；购买提示只保留**买家本人**与**导演**视角。
- 该改动当日未单独提交，随后续代码状态落地（8-13 会话复查确认行为正确）。

## 二、队友系统（8-13 ~ 8-14）

**规则与判定**
- `TeammateBehavior` 位掩码配置：bit1 队友免伤、bit2 搜索过滤队友、bit8 队友传物（`teammate_behavior` 字段，运行时可用导演动作修改）。
- `GameState::are_teammates()`：team_id 相同且 > 0 才算同队；team_id=0 表示无队，自己不算自己的队友。

**后端行为**
- 导演动作 `set_teammate_behavior`：游戏中随时开关队友模式。
- 队友免伤：普通攻击、橙武溅射（aoe_damage）、遥控地雷均不对队友生效。
- 搜索过滤：bit2 开启后搜索结果不再出现队友。
- 玩家动作 `transfer_item`：向同队队友转移背包道具，不限距离，**接收者体力 -5**；目标背包满 / 死亡 / 体力不足时拒绝；bit8 未开启时整体禁用。

**前端**
- 导演端：TeammateModeCard 配置卡片（总开关 + 三项行为配置）；规则预览展示；演员列表暴露 team_id。
- 演员端：道具转移按钮 + 队友选择对话框（排除自己）。

**测试**：`teammate_system_integration.rs` 21 个用例（免伤/溅射/地雷/搜索过滤/转移动作全路径）。

## 三、修复与杂项（8-14）

- 转移日志按受众区分措辞（发送方 / 接收方 / 导演各自视角），导演侧日志落库。
- 修复手机端软键盘与对话框对齐问题；转移对话框单选列表左对齐。
- 修复打开规则预览对话框时不刷新游戏数据的问题。
- 规则预览去掉队友行为细节标签（避免冗余）。
- `.gitignore` 增加本地部署 / 会话导出产物忽略规则。

## 四、售出系统（8-17）

**数据模型**
- `SellPriceEntry { id, rarity, price }`，`GameState.sell_prices`：同一稀有度最多一条。
- `Player.coins` 由整数改为 `f64`（支持 0.5 步长售出价）；货币显示格式化。

**后端**
- 导演动作 `sell_set_price` / `sell_remove_price`：对绿/蓝/紫/橙四档设置/移除售出价（复用 `GameState::rarity_display_name` 中文名映射）。
- 玩家动作 `sell_item`：仅**白天（非夜间行动时间）**可用；仅武器/防具；已装备道具不可售；按道具稀有度对应价格入账。

**前端**
- 导演端：SellSystemCard 配置卡片（四档价格增改删）。
- 演员端：售出按钮（与商店按钮互补——商店夜间用、售出白天用）+ SellItemDialog 背包武器/防具选择对话框。

**测试**：`sell_system_integration.rs` 13 个用例（定价/移除/白天窗口/装备限制/入账/广播 JSON）。

## 五、商店稀有度类目改造（8-17）

**动机**：避免场上出现同名称武器/防具——导演不再上架具体武器/防具，改为按稀有度类目定价，购买时后端随机抽取。

**数据模型**
- `ShopListing` 新增可选 `item_kind`（weapon|armor）与 `rarity`（common|rare|epic|legendary），同时 Some = 稀有度随机条目；旧存档缺省字段 = 具体物品条目（兼容可购）。
- 稀有度条目 `item_name` 存固定展示文案，如"绿类武器（随机）"。

**后端**
- 导演动作 `shop_list_rarity`：校验链——类别/稀有度合法、价格 i32 ≥ 1、道具库该类目非空、**库存 ≤ 该稀有度名称总数**、同类目唯一（重复 → "该类目已上架"）；成功广播全体。
- `shop_list_item` 增加限制：武器/防具具体上架被拒（"武器和防具请按稀有度类目上架"）；消耗品/功能物品/升级道具/货币照旧。
- `shop_buy` 扩展：稀有度条目购买时从候选名池随机抽取，**全局去重**（剔除所有玩家背包/已装备/地面道具中已存在的名字 + 本次购买已抽中的名字）；候选耗尽 → 整笔原子取消（Info 仅发买家，零状态变更）；沿用限量库存语义（卖出一件减一件，归零自动移除）。

**前端**
- 导演端 ShopManagement：上架对话框改为"上架类型"优先（具体物品 / 武器按稀有度 / 防具按稀有度）；稀有度选择器只列**未上架且道具库非空**的类目；数量上限联动该稀有度名称池大小。
- 演员端：商店列表结构不变，稀有度条目加色标（绿/蓝/紫/橙）；导演列表同样加色标。
- store 新增 `shopListRarity`；TS 类型 `ShopListing` 加可选字段。

**测试**：`shop_rarity_integration.rs` 11 个用例；全套后端测试 117 个 + `pnpm build` 全绿；整分支审查（opus）判定 READY_TO_MERGE。

## 六、售出绿色配对约束（8-17）

**动机**：绿色（common）武器/防具不能单独售出——一笔售出必须**恰好包含 2 件绿色物品**；蓝/紫/橙仍一次 1 件。计价不变，一对绿色共得 2× 绿价。

**后端**
- 载荷扩展：`PlayerActionParams` 新增 `item_ids: Option<Vec<String>>`（原 `item_id` 保留，装备/使用/丢弃等其他动作仍用）；`sell_item` 分支改读数组；`handle_sell_item_action(player_id, item_ids: &[String])`。
- 校验链（任一失败 → Info 仅发卖家，零状态变更）：时间窗 → 长度 ∈ {1,2} → **重复 id 拒绝**（"不能重复选择同一件物品"，防一件绿色重复计价漏洞）→ 逐件背包/武器防具/已定价 → 配对约束（len=1 且绿 → "绿色物品需成对售出"；len=2 且任一件非绿 → "绿色物品不能与其他稀有度混合售出"）。
- 原子应用：一次 `retain` 移除全部命中道具，`coins += 单价之和`；卖家/导演各一条合并消息（`你售出了 A、B，获得 X 货币`）。

**前端（SellItemDialog.vue）**
- radio 单选改 **checkbox 多选**；store `sellItem(itemIds: string[])` 发 `{ item_ids }` 载荷。
- 实时校验提示：仅 1 绿 → 提示再勾 1 件绿色；绿+非绿混选 → 混合售出提示；≥2 非绿 → 一次只能 1 件；>2 绿 → 一次只能 2 件；确认按钮仅校验通过时可用。

**测试**：`sell_system_integration.rs` 13 → 20 个用例（绿对成功入账/单绿拒绝/混合拒绝/双蓝拒绝/长度非法/对中缺件拒绝/重复 id 拒绝 + 原有回归）；全套后端 126 个 + `pnpm build` 全绿；整分支审查（opus）判定 READY_TO_MERGE。

## 七、部署与配套（8-17，未提交）

- 本地部署验证通过：后端 127.0.0.1:3000（.env 配 MySQL），前端 Vite http://localhost:5173/royale-arena/（代理 `/royale-arena/api`）。
- `full-feature-rules-template.json`：全功能规则模板（武器绿8/蓝21/紫12/橙9名、防具绿6/蓝5/紫5/橙5名、消耗品/功能道具/升级器/货币齐全、升级配方完整、`teammate_behavior=11` 三项全开）。
- 对话存档：`对话记录-2026-08-17.txt`。
- 本地 main 领先 origin/main 37 个提交，**尚未推送**。

---

## 附：提交清单

```
08-13  e7dd13a  docs: add teammate system design spec
08-13  61c5608  docs: add teammate system implementation plan
08-13  a702671  feat(teammate): add TeammateBehavior helpers and GameState::are_teammates
08-13  dd27994  feat(teammate): add director set_teammate_behavior action
08-13  6ca6fef  feat(teammate): block attack on teammates when damage immune bit set
08-13  a477711  feat(teammate): filter teammates out of splash and remote mine damage
08-13  2e57f49  feat(teammate): filter teammates from search candidates when bit 2 set
08-13  ca2f403  feat(teammate): add transfer_item player action
08-13  f258b44  feat(teammate): expose team_id in actor player list JSON
08-13  2380cb0  feat(teammate): add TeammateModeCard for director config
08-13  c71f1c4  feat(teammate): add transfer button + teammate picker dialog for actors
08-13  9953f2c  fix(teammate): exclude self from transfer picker
08-14  b63d98c  fix(director): refresh game data when opening rules preview dialog
08-14  49d0765  fix: transfer logs per-audience, director-only log entries, mobile keyboard, dialog alignment
08-14  cf4a7f7  tweak(teammate): keep original receiver transfer log wording
08-14  e67eb7e  ui(director): drop teammate behavior detail tags from rules preview
08-14  da3aad1  chore: ignore local deployment and session export artifacts
08-17  0fa5f39  docs: add sell system design spec
08-17  9caf40d  docs: add sell system implementation plan
08-17  6fbf8d1  feat(sell): add SellPriceEntry model, sell_prices state, f64 coins
08-17  444cdd9  feat(sell): director sell_set_price/sell_remove_price actions
08-17  1e8ab73  feat(sell): player sell_item action with day-window validation
08-17  f1834f9  feat(sell): broadcast sell_prices, frontend types and store actions
08-17  b132a65  feat(sell): director SellSystemCard for rarity price config
08-17  20c310b  feat(sell): actor sell button with item picker dialog
08-17  c448dec  docs: add shop rarity rework design spec
08-17  73f4b2b  docs: add shop rarity rework implementation plan
08-17  e00c9d3  docs: fix arithmetic and assertion clarity in shop rarity plan
08-17  3810526  feat(shop): rarity-class listings with pool validation, reject exact weapon/armor listing
08-17  830c652  feat(shop): random draw with global name dedup for rarity purchases
08-17  8a04c0e  feat(shop): rarity-class listing UI with pool-aware quantity cap
08-17  755652d  docs: add sell green-pair constraint design spec
08-17  88ba608  docs: add sell green-pair implementation plan
08-17  c9ed776  feat(sell): green items must be sold in pairs via multi-item sell_item
08-17  43c49d8  fix(sell): reject duplicate item ids in one sale
08-17  d394c5a  feat(sell): checkbox multi-select dialog with green-pair validation
08-17  2a3af60  fix(sell): frontend hint when more than two greens selected
```

设计/计划文档位于 `docs/superpowers/specs/` 与 `docs/superpowers/plans/`（售出系统、商店稀有度改造、售出绿色配对各一套）。
